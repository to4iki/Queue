
language: objective-c

install:
- gem install xcpretty --no-rdoc --no-ri --no-document --quiet

script:
- set -o pipefail && xcodebuild test -workspace Queue.xcodeproj -scheme Queue -sdk iphonesimulator ONLY_ACTIVE_ARCH=NO | xcpretty -c
